CREATE TABLE transactions (
    '''Denise'''
	item_# INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
	amount DECIMAL NOT NULL
    category text NOT NULL
    date DATE NOT NULL
    description text NOT NULL
);
